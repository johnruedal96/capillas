[project]
name = "capillas-shared"
version = "0.1.0"
description = "Shared types and protos for Capillas microservices"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9.0",
    "grpcio>=1.67.0",
]
